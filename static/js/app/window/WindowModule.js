'use strict';

var module = angular.module('window_module', [
    'window_service'
]);

module.controller('ssHistoricalCtrl', [
    '$scope',
    '$rootScope',
    '_', 
    function($scope, $rootScope, _){
        
    }
])